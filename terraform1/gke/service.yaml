apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
spec:
  type: LoadBalancer
  selector:
    app: nginx
    version: blue  # Initially points to blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

